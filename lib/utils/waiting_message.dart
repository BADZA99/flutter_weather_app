const List<String> messages = [
  'Nous téléchargeons les données…',
  'C’est presque fini 3. ',
  'Plus que quelques secondes…',
];



/*
 return Text(
                    'Weather: ${jsonDecode(jsonData)['name']}, '
                    'Date: ${DateTime.fromMillisecondsSinceEpoch(jsonDecode(jsonData)['dt'] * 1000)}, '
                    'Temperature: ${jsonDecode(jsonData)['main']['temp']}, '
                    'Humidity: ${jsonDecode(jsonData)['main']['humidity']}, '
                    'Wind Speed: ${jsonDecode(jsonData)['wind']['speed']}, '
                    'Sunrise: ${DateTime.fromMillisecondsSinceEpoch(jsonDecode(jsonData)['sys']['sunrise'] * 1000)}, '
                    'Sunset: ${DateTime.fromMillisecondsSinceEpoch(jsonDecode(jsonData)['sys']['sunset'] * 1000)}, '
                    'Degree: ${jsonDecode(jsonData)['wind']['deg']}',
                    style: const TextStyle(fontSize: 24),
                  ); */